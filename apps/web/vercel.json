{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "bash ../../scripts/check-migration-safety.sh && prisma migrate deploy && prisma generate && pnpm run build",
  "outputDirectory": ".next"
}